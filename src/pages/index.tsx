import SearchPage, { getResultPageProps } from '../components/ResultPage'

// TODO: replace this with home page component if needed
export default SearchPage

export const getServerSideProps = getResultPageProps()
